@model IEnumerable<courseworkv5.Administrators>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Administrators</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Workers.WorkerName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Workers.WorkerName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AdministratorId }) |
            @Html.ActionLink("Details", "Details", new { id=item.AdministratorId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AdministratorId })
        </td>
    </tr>
}

</table>
