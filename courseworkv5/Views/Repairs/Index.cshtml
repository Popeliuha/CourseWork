@model IEnumerable<courseworkv5.Repair>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Repairs</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Repair1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DateOfRepair)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Trains.TrainNumber)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Repair1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DateOfRepair)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Trains.TrainNumber)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RepairId }) |
            @Html.ActionLink("Details", "Details", new { id=item.RepairId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RepairId })
        </td>
    </tr>
}

</table>
